CREATE TABLE server(
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    url TEXT NOT NULL
);

selectAll:
SELECT * FROM server;

insert:
INSERT INTO server(name, url) VALUES ?;

delete:
DELETE FROM server WHERE id = ?;
